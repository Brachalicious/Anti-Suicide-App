# Optional: frontend API base URL for split frontend/backend hosting.
# Leave blank when using the same-origin Express server or Netlify function redirects.
# VITE_API_BASE_URL=https://your-api-host.example.com

# Server Port (default: 3000)
# PORT=3000
